# Linked

An `ai/` doc reachable from the routing seed — not an orphan.

It links onward to [deep](./deep.md), so reachability is multi-hop: `deep.md`
is reached only through this non-seed doc, not directly from a routing table.
