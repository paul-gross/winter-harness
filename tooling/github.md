# GitHub CLI

**GitHub interactions** must use the [`gh`](https://cli.github.com/) CLI — issues, PRs, releases, repo management. Never
script against the web UI.

The full winter-side workflow lives in `winter-github:/context/gh-cli.md` and `workspace:/context/github.md` (canonical
label set).
